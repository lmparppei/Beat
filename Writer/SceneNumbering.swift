//
//  SceneNumbering.swift
//  Writer
//
//  Created by Lauri-Matti Parppei on 23.8.2018.
//  Copyright © 2018 Hendrik Noeller. All rights reserved.
//

/*
import Foundation

@objc public class SceneNumbering: NSObject {
 

    func findScenes(text: String) -> String {
        //var lines: [String] = []
        var position = 0
    
        var lines = text.components(separatedBy: "\n")
        var regex = "(\n)(([iI][nN][tT]|[eE][xX][tT]|[^\\w][eE][sS][tT]|\\.|[iI]\\.?\\/[eE]\\.?)([^\\n]+))\\n{2}"
    
        lines.forEach { line in
        
            //if line.range(of: regex, options: .regularExpression, range: nil, locale: nil) != nil
            // var matches = regex.matchesInString(line)
            if (line.range(of: regex, range: nil, locale: nil) != nil) {
                NSLog(line)
            }
        }
    
        return ""
    }
    
}
 */
